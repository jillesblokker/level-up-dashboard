'use client' /** * GameStoreProvider * * Drop this inside your root layout (inside ClerkProvider / body). * It hydrates the Zustand game store on mount and keeps it in sync * with the CharacterStatsService event bus. * * Usage in layout.tsx: * <GameStoreProvider /> (no children needed, it renders nothing) */ import { useEffect } from 'react'
import { useGameStore } from '@/stores/game-store' export function GameStoreProvider() { const hydrate = useGameStore(s => s.hydrate) const syncFromStats = useGameStore(s => s.syncFromStats) const hydrated = useGameStore(s => s.hydrated) // Hydrate on first mount useEffect(() => { if (!hydrated) { hydrate() } }, [hydrated, hydrate]) // Listen for CharacterStatsService broadcasts and keep store in sync useEffect(() => { const handleStatsUpdate = () => { try { const { getCharacterStats } = require('@/lib/character-stats-service') syncFromStats(getCharacterStats()) } catch { // Silently ignore — stats service may not be available yet } } window.addEventListener('character-stats-update', handleStatsUpdate) return () => window.removeEventListener('character-stats-update', handleStatsUpdate) }, [syncFromStats]) return null // This component renders nothing
}
