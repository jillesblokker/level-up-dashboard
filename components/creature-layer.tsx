import { logger } from "@/lib/logger";
import React, { useState, useEffect, useRef, useCallback } from 'react';
import { CreatureDefinition, CREATURE_DEFINITIONS } from '@/lib/creature-mapping';
import { CreatureSprite } from './creature-sprite';
import { Tile } from '@/types/tiles';
import { useUser } from '@clerk/nextjs';
import { useCitizensStore, isCitizenHungry, isHarvestReady, FOOD_DAYS_MAP, Citizen } from '@/stores/citizensStore';
import { getInventory } from '@/lib/inventory-manager';
import { loadTileInventory } from '@/lib/data-loaders';
import { useToast } from '@/components/ui/use-toast';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription } from '@/components/ui/dialog';
import { Button } from '@/components/ui/button';
import { Heart, Sparkles, Star, Clock, Coins } from 'lucide-react';
import Image from 'next/image'; interface CreatureLayerProps { grid: Tile[][]; tileSize: number; mapType: 'kingdom' | 'realm'; playerPosition?: { x: number; y: number }; // Optional: actual player position for tooltips onCreatureClick?: (creature: ActiveCreature) => void;
} interface ActiveCreature { instanceId: string; definitionId: string; position: { row: number; col: number }; targetPosition: { row: number; col: number }; state: 'idle' | 'walking';
} export function CreatureLayer({ grid, mapType, playerPosition, onCreatureClick }: Omit<CreatureLayerProps, 'tileSize'>) { const [activeCreatures, setActiveCreatures] = useState<ActiveCreature[]>([]); const [playerTile, setPlayerTile] = useState<{ row: number; col: number } | null>(null); const { user, isLoaded } = useUser(); const containerRef = useRef<HTMLDivElement>(null); const { toast } = useToast(); const loadCitizens = useCitizensStore(state => state.loadCitizens); const citizens = useCitizensStore(state => state.citizens); const feedCitizen = useCitizensStore(state => state.feedCitizen); const harvestCitizen = useCitizensStore(state => state.harvestCitizen); const [selectedCitizenId, setSelectedCitizenId] = useState<string | null>(null); const [isModalOpen, setIsModalOpen] = useState(false); const [inventoryFoods, setInventoryFoods] = useState<{ id: string; name: string; quantity: number; emoji: string }[]>([]); const [isInteracting, setIsInteracting] = useState(false); const [quoteIndex, setQuoteIndex] = useState(0); // Get the selected citizen object from the store dynamically const selectedCitizen = citizens.find(c => c.id === selectedCitizenId) || null; // Rotate quotes every 2 seconds when modal is open useEffect(() => { if (!isModalOpen || !selectedCitizen || !selectedCitizen.greetings || selectedCitizen.greetings.length <= 1) { return; } // Reset to 0 when opening a new citizen const interval = setInterval(() => { setQuoteIndex(prev => (prev + 1) % selectedCitizen.greetings.length); }, 2000); return () => clearInterval(interval); }, [isModalOpen, selectedCitizen]); // Update player tile when playerPosition prop changes useEffect(() => { if (playerPosition) { setPlayerTile({ row: playerPosition.y, col: playerPosition.x }); } }, [playerPosition]); // Load citizens when user is available useEffect(() => { if (isLoaded && user?.id) { loadCitizens(user.id); } }, [isLoaded, user?.id, loadCitizens]); // Sync active, fed citizens to maps (capped at 12, prioritizing favorites) useEffect(() => { if (!isLoaded || !user) return; if (citizens.length === 0) return; // Get all active and fed citizens (excluding animals, which are handled separately) const activeFed = citizens.filter(c => c.active); // Prioritize favorites and cap at 12 const prioritized = [...activeFed].sort((a, b) => { if (a.favorite && !b.favorite) return -1; if (!a.favorite && b.favorite) return 1; return 0; }); const capped = prioritized.slice(0, 12); const cappedIds = new Set(capped.map(c => c.id)); setActiveCreatures(prev => { // Keep existing creatures that are still active/fed/capped const next = prev.filter(c => cappedIds.has(c.definitionId)); // Find which capped citizens are NOT yet spawned const spawnedIds = new Set(next.map(c => c.definitionId)); const toSpawn = capped.filter(c => !spawnedIds.has(c.id)); toSpawn.forEach(c => { const spawnPoint = findRandomSpawnPoint(grid, c.type); if (spawnPoint) { next.push({ instanceId: `${c.id}-${Date.now()}-${Math.random()}`, definitionId: c.id, position: spawnPoint, targetPosition: spawnPoint, state: 'idle' }); } }); return next; }); }, [citizens, grid, isLoaded, user]); // Wandering Logic - Different for Kingdom vs Realm useEffect(() => { if (activeCreatures.length === 0) return; const moveCreature = (creature: ActiveCreature) => { const def = citizens.find(c => c.id === creature.definitionId) || CREATURE_DEFINITIONS[creature.definitionId]; if (!def) return creature; const neighbors = getNeighbors(creature.position, grid); const validNeighbors = neighbors.filter(n => n.tile && isHabitatMatch(n.tile, def.type)); if (validNeighbors.length > 0) { const nextTile = validNeighbors[Math.floor(Math.random() * validNeighbors.length)]; if (nextTile) { return { ...creature, position: { row: nextTile.row, col: nextTile.col }, state: 'walking' as const }; } } return creature; }; if (mapType === 'kingdom') { // Kingdom: Each creature moves independently at random intervals (2-10 seconds) const timers: NodeJS.Timeout[] = []; activeCreatures.forEach((creature, index) => { const scheduleNextMove = () => { const delay = Math.random() * 8000 + 2000; // 2-10 seconds const timer = setTimeout(() => { setActiveCreatures(prev => { const newCreatures = [...prev]; if (newCreatures[index]) { newCreatures[index] = moveCreature(newCreatures[index]); } return newCreatures; }); scheduleNextMove(); // Schedule next move }, delay); timers.push(timer); }; scheduleNextMove(); }); return () => { timers.forEach(timer => clearTimeout(timer)); }; } else { // Realm: All creatures move together every 5 seconds (like animals) const interval = setInterval(() => { setActiveCreatures(prev => prev.map(moveCreature)); }, 5000); return () => clearInterval(interval); } }, [grid, activeCreatures.length, mapType, citizens]); // Track Player Position (Kingdom Mouse Hover or Realm playerPosition) useEffect(() => { if (mapType === 'realm') return; // Realm uses playerPosition prop const handleGlobalMouseMove = (e: MouseEvent) => { if (!containerRef.current) return; const rect = containerRef.current.getBoundingClientRect(); // Check if mouse is inside the grid if ( e.clientX >= rect.left && e.clientX <= rect.right && e.clientY >= rect.top && e.clientY <= rect.bottom ) { const x = e.clientX - rect.left; const y = e.clientY - rect.top; if (!grid || grid.length === 0 || !grid[0]) return; const cols = grid[0].length; const rows = grid.length; const tileWidth = rect.width / cols; const tileHeight = rect.height / rows; const col = Math.floor(x / tileWidth); const row = Math.floor(y / tileHeight); setPlayerTile({ row, col }); } else { setPlayerTile(null); } }; window.addEventListener('mousemove', handleGlobalMouseMove); return () => window.removeEventListener('mousemove', handleGlobalMouseMove); }, [grid, mapType]); const loadLayerInventoryFood = async (userId: string) => { const inv = await getInventory(userId); const foodItems = inv .filter(item => FOOD_DAYS_MAP[item.id] !== undefined && item.quantity > 0) .map(item => ({ id: item.id, name: item.name, quantity: item.quantity, emoji: item.emoji || '🐟' })); const tileInv = await loadTileInventory(userId); if (tileInv && typeof tileInv === 'object') { Object.entries(tileInv).forEach(([rawKey, value]) => { const key = rawKey === 'water' ? 'material-water' : rawKey; if (FOOD_DAYS_MAP[key] !== undefined && value && value.quantity > 0) { const name = key === 'material-water' ? 'Water' : (value.name || key); const emoji = key === 'material-water' ? '💧' : (value.emoji || '📦'); foodItems.push({ id: key, name, quantity: value.quantity, emoji }); } }); } return foodItems; }; const handleCreatureClick = async (creature: ActiveCreature) => { // Trigger generic callback if provided onCreatureClick?.(creature); const citizen = citizens.find(c => c.id === creature.definitionId); if (!citizen) return; setSelectedCitizenId(citizen.id); setQuoteIndex(0); // Reset quote when opening setIsModalOpen(true); if (user?.id) { try { const foodItems = await loadLayerInventoryFood(user.id); setInventoryFoods(foodItems); } catch (error) { logger.error('Failed to load inventory food for modal:', error); } } }; const handleFeed = async (foodId: string) => { if (!user?.id || !selectedCitizenId) return; setIsInteracting(true); try { const success = await feedCitizen(user.id, selectedCitizenId, foodId); if (success) { const feedEmoji = foodId === 'material-water' ? '💧' : '🐟'; toast({ title: `Citizen Fed ${feedEmoji}`, description: `${selectedCitizen?.name} has been fed! They are happy and active.`, }); // Dispatch event so other pages (like Character inventory) sync up window.dispatchEvent(new Event('character-inventory-update')); // Reload food const foodItems = await loadLayerInventoryFood(user.id); setInventoryFoods(foodItems); } else { toast({ title: "Feeding Failed", description: "You don't have enough food in your inventory.", variant: "destructive" }); } } catch (error) { logger.error('Failed to feed citizen:', error); } finally { setIsInteracting(false); } }; const getCitizenSynergy = useCallback((citizenId: string) => { const citizen = citizens.find(c => c.id === citizenId); if (!citizen) return { multiplier: 1, elementMatch: false, roadMatch: false, tileName: 'Vacant' }; const creature = activeCreatures.find(c => c.definitionId === citizenId); if (!creature) return { multiplier: 1, elementMatch: false, roadMatch: false, tileName: 'Vacant' }; const tile = grid[creature.position.row]?.[creature.position.col]; if (!tile) return { multiplier: 1, elementMatch: false, roadMatch: false, tileName: 'Vacant' }; const elementMatch = isHabitatMatch(tile, citizen.type); const neighbors = getNeighbors(creature.position, grid); const roadMatch = neighbors.some(n => n.tile && ( n.tile.type?.toLowerCase()?.includes('road') || n.tile.type?.toLowerCase()?.includes('path') )); let multiplier = 1; if (elementMatch) multiplier += 0.25; if (roadMatch) multiplier += 0.10; return { multiplier, elementMatch, roadMatch, tileName: tile.name || tile.type || 'Vacant' }; }, [citizens, activeCreatures, grid]); const handleHarvest = async (multiplier?: number) => { if (!user?.id || !selectedCitizenId || !selectedCitizen) return; setIsInteracting(true); try { const success = await harvestCitizen(user.id, selectedCitizenId, multiplier); if (success) { toast({ title: "Harvest Collected! 🪙", description: `Successfully collected daily rewards from ${selectedCitizen.name}!`, }); // Dispatch event for any other listeners window.dispatchEvent(new Event('character-inventory-update')); setIsModalOpen(false); } } catch (error) { logger.error('Failed to harvest citizen:', error); } finally { setIsInteracting(false); } }; if (!grid || grid.length === 0 || !grid[0]) return null; const rows = grid.length; const cols = grid[0].length; return ( <div ref={containerRef} className="absolute inset-0 pointer-events-none z-10" > {activeCreatures.map(creature => { const citizen = citizens.find(c => c.id === creature.definitionId); const def = citizen || CREATURE_DEFINITIONS[creature.definitionId]; if (!def) { logger.warn('[CreatureLayer] No definition found for creature:', creature.definitionId); return null; } const isPlayerOnTile = !!playerTile && playerTile.row === creature.position.row && playerTile.col === creature.position.col; return ( <div key={creature.instanceId} className="absolute transition-all ease-in-out pointer-events-none z-20" style={{ top: `${(creature.position.row / rows) * 100}%`, left: `${(creature.position.col / cols) * 100}%`, width: `${(1 / cols) * 100}%`, height: `${(1 / rows) * 100}%`, transitionDuration: '3000ms' }} > {/* Smaller Hit Area for Interaction */} <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[60%] h-[60%] pointer-events-auto cursor-pointer hover:scale-125 transition-transform z-30 flex items-center justify-center rounded-full" onClick={(e) => { e.stopPropagation(); handleCreatureClick(creature); }} /> {/* Visual Sprite */} <div className="absolute inset-x-0 bottom-0 top-0"> <CreatureSprite creature={def} isPlayerOnTile={isPlayerOnTile} tileSize={100} isFavorite={citizen?.favorite || false} isHarvestReady={citizen ? isHarvestReady(citizen) : false} /> </div> </div> ); })} {/* Citizen Interaction Modal */} <Dialog open={isModalOpen} onOpenChange={setIsModalOpen}> <DialogContent className="w-[min(90vw,420px)] max-w-none p-0 overflow-hidden shadow-2xl rounded-2xl bg-gradient-to-b from-zinc-900 via-zinc-950 to-zinc-950 border border-zinc-800 shadow-amber-500/5 text-white" > <DialogHeader className="sr-only"> <DialogTitle>{selectedCitizen?.name || "Citizen Interaction"}</DialogTitle> <DialogDescription>Interact with your realm citizen</DialogDescription> </DialogHeader> {/* Ambient category glow */} <div className="absolute inset-0 pointer-events-none"> <div className={`absolute top-1/4 left-1/2 -translate-x-1/2 w-48 h-48 rounded-full blur-3xl opacity-20 ${ selectedCitizen?.type === 'fire' ? 'bg-red-500' : selectedCitizen?.type === 'water' ? 'bg-blue-500' : selectedCitizen?.type === 'earth' ? 'bg-amber-600' : selectedCitizen?.type === 'nature' ? 'bg-emerald-500' : selectedCitizen?.type === 'ice' ? 'bg-cyan-400' : selectedCitizen?.type === 'monster' ? 'bg-purple-600' : 'bg-yellow-500' }`} /> </div> <div className="relative z-10 flex flex-col items-center pt-8 pb-4 px-6"> {/* Portrait */} <div className="relative w-32 h-32 flex items-center justify-center bg-zinc-900 rounded-full border-2 border-zinc-800 shadow-xl overflow-hidden p-4"> {selectedCitizen && ( <div className="w-24 h-24 relative"> <Image src={selectedCitizen.isMythic ? `/images/Mythics/${selectedCitizen.filename}?v=2` : `/images/creatures/${selectedCitizen.filename}` } alt={selectedCitizen.name} fill sizes="96px" className="object-contain drop-shadow-md" onError={(e) => { logger.warn('Failed to load modal citizen image:', selectedCitizen.name); }} /> </div> )} {selectedCitizen?.favorite && ( <div className="absolute top-1 right-1 bg-amber-500 text-black rounded-full p-1 border border-yellow-300 shadow z-20"> <Star className="w-3 h-3 fill-current" /> </div> )} </div> {/* Title & Habitat badge */} <h2 className="mt-4 text-2xl font-serif font-semibold text-center text-amber-500"> {selectedCitizen?.name} </h2> <div className="mt-1 flex items-center gap-1.5"> <span className={`text-[10px] uppercase font-bold tracking-wider px-2 py-0.5 rounded-full border ${ selectedCitizen?.type === 'fire' ? 'bg-red-950/40 text-red-400 border-red-900/50' : selectedCitizen?.type === 'water' ? 'bg-blue-950/40 text-blue-400 border-blue-900/50' : selectedCitizen?.type === 'earth' ? 'bg-amber-950/40 text-amber-600 border-amber-900/50' : selectedCitizen?.type === 'nature' ? 'bg-emerald-950/40 text-emerald-400 border-emerald-900/50' : selectedCitizen?.type === 'ice' ? 'bg-cyan-950/40 text-cyan-400 border-cyan-900/50' : selectedCitizen?.type === 'monster' ? 'bg-purple-950/40 text-purple-400 border-purple-900/50' : 'bg-yellow-950/40 text-yellow-400 border-yellow-900/50' }`}> {selectedCitizen?.type} Citizen </span> {selectedCitizen?.isMythic && ( <span className="text-[10px] uppercase font-bold tracking-wider px-2 py-0.5 rounded-full bg-purple-900/40 text-purple-300 border border-purple-800/50 flex items-center gap-0.5"> <Sparkles className="w-2.5 h-2.5" /> Mythic </span> )} </div> {/* Speech Bubble / Greeting */} <div className="mt-4 bg-zinc-900 border border-zinc-800/60 rounded-xl px-4 py-2.5 text-zinc-300 text-xs text-center italic max-w-xs relative transition-opacity duration-300"> &ldquo;{selectedCitizen?.greetings[quoteIndex % (selectedCitizen?.greetings.length || 1)]}&rdquo; <div className="absolute -top-1.5 left-1/2 -translate-x-1/2 w-3 h-3 bg-zinc-900 rotate-45 border-t border-l border-zinc-800/60" /> </div> </div> {/* Actions Panel */} <div className="relative z-10 px-6 pb-6 flex flex-col gap-2"> {/* Synergy Info */} {(() => { const synergy = selectedCitizen ? getCitizenSynergy(selectedCitizen.id) : { multiplier: 1, elementMatch: false, roadMatch: false, tileName: 'Vacant' }; return ( <div className="bg-zinc-900 border border-zinc-800/40 rounded-xl p-2.5 flex flex-col gap-1 text-[11px] mb-1"> <div className="flex justify-between items-center text-zinc-400"> <span>Current Tile:</span> <span className="font-semibold text-zinc-200 capitalize">{synergy.tileName}</span> </div> <div className="flex flex-wrap gap-1 mt-1"> {synergy.elementMatch && ( <span className="bg-green-950/40 text-green-400 border border-green-900/50 px-1.5 py-0.5 rounded text-[10px] font-medium"> 🌟 Element Synergy (+25%) </span> )} {synergy.roadMatch && ( <span className="bg-amber-950/40 text-amber-400 border border-amber-900/50 px-1.5 py-0.5 rounded text-[10px] font-medium"> 🛣️ Trade Synergy (+10%) </span> )} {!synergy.elementMatch && !synergy.roadMatch && ( <span className="text-zinc-500 italic">No active synergies</span> )} </div> </div> ); })()} {/* Cooldown or Harvest Button */} {selectedCitizen && isHarvestReady(selectedCitizen) ? ( <Button onClick={() => { const synergy = getCitizenSynergy(selectedCitizen.id); handleHarvest(synergy.multiplier); }} disabled={isInteracting} className="w-full h-11 bg-amber-500 hover:bg-amber-600 text-black font-serif font-bold rounded-xl flex items-center justify-center gap-2 shadow-[0_0_15px_rgba(245,158,11,0.5)] hover:shadow-[0_0_25px_rgba(245,158,11,0.7)] transition-all hover:-translate-y-0.5" > <Coins className="w-4 h-4" /> Collect Gold 💰 </Button> ) : selectedCitizen && !isCitizenHungry(selectedCitizen) ? ( <div className="bg-zinc-950 border border-zinc-800/40 rounded-xl p-3 flex flex-col items-center justify-center gap-1"> <span className="text-xs text-zinc-400">Next gold harvest available in:</span> <div className="text-amber-500 font-semibold text-sm flex items-center gap-1"> <Clock className="w-3.5 h-3.5" /> {(() => { if (!selectedCitizen.lastHarvestedAt) return "Ready!"; const nextHarvest = new Date(selectedCitizen.lastHarvestedAt).getTime() + 24 * 60 * 60 * 1000; const diff = nextHarvest - Date.now(); if (diff <= 0) return "Ready!"; const hrs = Math.floor(diff / (1000 * 60 * 60)); const mins = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60)); return `${hrs}h ${mins}m`; })()} </div> </div> ) : null} {/* Feeding Section if Hungry */} {selectedCitizen && isCitizenHungry(selectedCitizen) && ( <div className="flex flex-col gap-2"> <div className="text-center text-xs text-red-400 font-semibold mb-1 flex items-center justify-center gap-1"> 😋 Needs Fish Food to wander map & produce gold! </div> {inventoryFoods.length === 0 ? ( <Button disabled className="w-full h-11 bg-zinc-800 text-zinc-500 rounded-xl border border-zinc-700"> No Fish Food in Inventory 🐟 </Button> ) : ( <div className="flex flex-col gap-1.5"> {inventoryFoods.map(food => ( <Button key={food.id} onClick={() => handleFeed(food.id)} disabled={isInteracting} className="w-full h-11 bg-orange-600 hover:bg-orange-700 text-white rounded-xl flex items-center justify-between px-4 shadow border border-orange-500/20" > <div className="flex items-center gap-2"> <span>{food.emoji}</span> <span className="text-xs font-semibold">{food.name} (x{food.quantity})</span> </div> <span className="text-[10px] bg-zinc-950 text-orange-200 px-2 py-0.5 rounded-md font-mono"> +{FOOD_DAYS_MAP[food.id]} Day{FOOD_DAYS_MAP[food.id] !== 1 ? 's' : ''} Active </span> </Button> ))} </div> )} </div> )} <Button variant="ghost" onClick={() => setIsModalOpen(false)} className="w-full h-10 text-zinc-400 hover:text-zinc-200 hover:bg-zinc-800/40 rounded-xl mt-1 text-xs" > Close </Button> </div> </DialogContent> </Dialog> </div> );
} // --- Helpers --- function findRandomSpawnPoint(grid: Tile[][], habitatType: string): { row: number; col: number } | null { if (!grid) return null; let validTiles: { row: number; col: number }[] = []; // First pass: Try strict habitat match grid.forEach((row, r) => { if (!row) return; row.forEach((tile, c) => { if (tile && isHabitatMatch(tile, habitatType)) { validTiles.push({ row: r, col: c }); } }); }); // Second pass: If no tiles found, try 'vacant' or 'grass' as fallback for ANY creature // This ensures creatures appear even if their specific habitat isn't built yet if (validTiles.length === 0) { // logger.debug(`[CreatureLayer] No specific habitat found for ${habitatType}, trying fallback to grass/vacant`); grid.forEach((row, r) => { if (!row) return; row.forEach((tile, c) => { const type = tile?.type?.toLowerCase() || 'vacant'; if (type === 'vacant' || type.includes('grass') || type.includes('dirt') || type.includes('plain')) { validTiles.push({ row: r, col: c }); } }); }); } if (validTiles.length === 0) return null; return validTiles[Math.floor(Math.random() * validTiles.length)] || null;
} function getNeighbors(pos: { row: number; col: number }, grid: Tile[][]) { if (!grid || grid.length === 0) return []; const rows = grid.length; const cols = grid[0]?.length || 0; const neighbors = []; // Check Up if (pos.row > 0) { const upRow = grid[pos.row - 1]; if (upRow && upRow[pos.col]) { neighbors.push({ row: pos.row - 1, col: pos.col, tile: upRow[pos.col] }); } } // Check Down if (pos.row < rows - 1) { const downRow = grid[pos.row + 1]; if (downRow && downRow[pos.col]) { neighbors.push({ row: pos.row + 1, col: pos.col, tile: downRow[pos.col] }); } } // Check Left if (pos.col > 0) { const currentRow = grid[pos.row]; if (currentRow && currentRow[pos.col - 1]) { neighbors.push({ row: pos.row, col: pos.col - 1, tile: currentRow[pos.col - 1] }); } } // Check Right if (pos.col < cols - 1) { const currentRow = grid[pos.row]; if (currentRow && currentRow[pos.col + 1]) { neighbors.push({ row: pos.row, col: pos.col + 1, tile: currentRow[pos.col + 1] }); } } return neighbors;
} function isHabitatMatch(tile: Tile, habitatType: string): boolean { // Map tile types to habitat types // Kingdom tiles: 'vacant', 'well', 'blacksmith', 'fisherman', 'sawmill', 'windmill', 'grocery', 'castle', 'temple', 'fountain', 'pond', 'foodcourt', 'vegetables', 'wizard', 'mayor', 'inn', 'library', 'mansion', 'jousting', 'archery', 'watchtower' // Realm tiles: 'grass', 'water', 'forest', 'mountain', 'desert', 'ice', 'snow', 'cave', 'dungeon', 'castle', 'lava', 'volcano', 'city', 'town', 'mystery' const tileType = tile.type?.toLowerCase() || 'vacant'; // logger.debug('[isHabitatMatch] Checking tile type:', tileType, 'against habitat:', habitatType); switch (habitatType) { case 'water': // Water creatures: ponds, fountains, fisherman areas, water tiles return tileType.includes('water') || tileType.includes('pond') || tileType.includes('fountain') || tileType.includes('fisherman') || tileType.includes('well') || tileType.includes('bridge') || tileType.includes('lake') || tileType.includes('river'); case 'fire': // Fire creatures: blacksmith, tavern, inn, kitchen areas, lava // Also allow grass/vacant so they aren't stuck if spawned there return tileType.includes('blacksmith') || tileType.includes('tavern') || tileType.includes('inn') || tileType.includes('foodcourt') || tileType.includes('lava') || tileType.includes('volcano') || tileType.includes('grass') || tileType === 'vacant'; case 'earth': // Earth creatures: mountains, mines, walls, roads, quarries // Also allow grass/vacant return tileType.includes('mountain') || tileType.includes('mine') || tileType.includes('wall') || tileType.includes('watchtower') || tileType.includes('road') || tileType.includes('grass') || tileType === 'vacant'; case 'nature': // Nature creatures: forests, farms, gardens, sawmills, windmills return tileType.includes('forest') || tileType.includes('farm') || tileType.includes('field') || tileType.includes('lumber') || tileType.includes('sawmill') || tileType.includes('windmill') || tileType.includes('vegetables') || tileType.includes('grass') || tileType === 'vacant'; case 'ice': // Ice creatures: ice tiles, snow, mountains, water (frozen) return tileType.includes('ice') || tileType.includes('snow') || tileType.includes('mountain') || tileType.includes('water'); case 'monster': // Monsters: dungeons, caves, castles, vacant areas return tileType.includes('dungeon') || tileType.includes('cave') || tileType.includes('castle') || tileType.includes('forest') || tileType.includes('grass') || tileType === 'vacant'; case 'special': // Special creatures: can appear anywhere return true; default: // Default: only vacant tiles return tileType === 'vacant' || tileType.includes('grass'); }
}
